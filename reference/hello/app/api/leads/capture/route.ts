// POST /api/leads/capture
//
// Convenience alias of POST /api/lead-captures using the route name from the
// prompt brief. Same behaviour — insert into lead_captures.

export { POST } from "@/app/api/lead-captures/route";
